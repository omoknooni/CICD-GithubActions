name: petclinic-pipeline
on:
    push: 
        branches: [main]
        paths:
            - 'simple_flask/**'
jobs:
    build:
        runs-on: ubuntu-latest
        outputs:
          image_tag: ${{ steps.docker_build.outputs.image_tag }}
        steps:
            # Repo checkout
            - name: Repo download
              uses: actions/checkout@v4

            # docker hub 로그인
            - name: Login Docker hub
              uses: docker/login-action@v2
              with:
                username: ${{ secrets.DOCKER_USERNAME }}
                password: ${{ secrets.DOCKER_PASSWORD }}

            # 빌드
            - name: Build Project
              run: |
                TAG=$(date +%Y%m%d%H%M%S)
                docker build -t ${{ secrets.DOCKER_USERNAME }}/simple_web:$TAG .

            # docker 이미지 push
            - name: Push Docker Image
              id: docker_build
              run: |
                docker push ${{ secrets.DOCKER_USERNAME }}/simple_web:$TAG
                echo "image_tag=$TAG" >> $GITHUB_OUTPUT


    # deploy:
    #     needs: build
    #     runs-on: ubuntu-latest
    #     steps:
    #       # checkout manifest repo
    #       - name: Checkout
    #         uses: actions/checkout@v4
    #         with:
    #           repository: omoknooni/argocd-test
    #           path: kubernetes

    #       # Update kubernetes manifest image tag
    #       - name: Update manifest
    #         run: |
    #           sed -i "s|image: ${{ secrets.DOCKER_USERNAME }}/spring-petclinic:.*|image: ${{ secrets.DOCKER_USERNAME }}/spring-petclinic:${{ env.IMAGE_TAG }}|" kubernetes/deployment.yml

    #       # Commit & Push manifest
    #       - name: Commit N Push manifest
    #         env:
    #           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #         run: |
    #           git config --global user.email "hihm0609@gmail.com"
    #           git config --global user.name "omoknooni"
    #           git add .
    #           git commit -m "Update image tag to ${{ needs.build.outputs.image_tag }}"
    #           git push origin main
